# RaoVids
Random youtube video aggregator and player for raocow. (wip, doesn't work yet. TODO: update readme :)

To run for debug mode, start the dev postgres instance with `docker-compose up -d` and then run the Visual Studio solution.
